`build.sh` script is for manually building the shared library.
Resulting `libCO2ComfortVentilation.so` or `libCO2ComfortVentilation.dylib` 
are copied to SIM-VICUS top-level `bin/release` directory.

